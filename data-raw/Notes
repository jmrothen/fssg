TBD:

Considerations for ancillary parameters.

Full fleshed out testing suite.

QQplot works for more complicated models (S ~ a + b).

Standardize the naming system of included distributions.

Flesh out 'fullnames'

Add string parsing to get_fssg_dist

Fix function defaults in fssg_dist.
  - if q, h, or H not included, estimate them using p/d

Add check in fssg; if d/p not specified, attempt to use h, H instead?




knitr::kable(read.csv('data-raw/fit_table.csv'), format='pipe')

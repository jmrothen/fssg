% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fssg.R
\name{fssg}
\alias{fssg}
\title{fssg: Flexsurv "Shotgun".}
\usage{
fssg(
  formula,
  data = NA,
  models = NA,
  skip = c("default"),
  opt_method = "BFGS",
  spline = NA,
  max_knots = 1,
  dump_models = TRUE,
  detailed = FALSE,
  ibs = FALSE,
  progress = TRUE,
  warn = FALSE
)
}
\arguments{
\item{formula}{Formula. Should be a survival formula, with a Surv object on the left hand side.}

\item{data}{If your formula needs a dataset, provide that here.}

\item{models}{Vector of strings. If you only want to run specific models, specify them here by their list name in \code{fssg_dist_list}.}

\item{skip}{Vector. If you want to skip any specific models, you can add their names here.
By default, some of the repetitive or incredibly niche models are skipped.}

\item{opt_method}{String. Named of the preferred optimization method. Default for fssg is 'Nelder-Mead', with 'BFGS' being used as a back-up in case of errors.
Can be any valid \code{optim} method, and the back-up method will always be 'BFGS', or 'Nelder-Mead' if 'BFGS' is the primary method provided.}

\item{spline}{String or Vector of Strings. Include 'rp' or 'wy' for
Royston-Parmar natural cubic spline, or Wang-Yan alternative natural cubic spline respectively.
The Wang-Yan version requires the package \code{splines2ns}.
If set to NA, then the spline step will be skipped.
\code{fssg} runs spline models using all three available scale options in \code{flexsurvspline}, for 'hazard', 'odds' and 'normal.}

\item{max_knots}{Integer. Specifies the maximum number of knots to be considered in spline models.}

\item{dump_models}{Logical. If TRUE, each successful model will be placed into a list and returned.}

\item{detailed}{Logical. If True, calculates a number of additional fit statistics for each model.}

\item{ibs}{Logical. If TRUE, calculate integrated brier score for each model.
Please note that this \emph{greatly} increases run time, and is not recommended for large data.}

\item{progress}{Logical. If TRUE, prints progress updates while the function runs.}

\item{warn}{Logical. If TRUE, also prints any warnings that appear.}
}
\value{
List containing a summary of the models generated. If dump_models is True, also returns a list of generated models.
}
\description{
A simple tool for the bulk creation and testing of parametric survival models.
}
\details{
Please see \code{vignette("fssg")} for a more in-depth example of the function.
}
\examples{
library(survival)
fssg(
  Surv(time, status)~1,
  data=aml,
  models=c('genf','exp','dagum','lomax','rayleigh','gamma_gompertz'),
  spline = c('rp'),
  max_knots=2,
  warn = TRUE
)

}
